Description: Apply git commit ID
Author: Léo Haf <leo@haf.ovh>
Forwarded: not-needed
Last-Update: 2025-06-02

--- bitcoin-knots-28.1.knots20250305.orig/src/clientversion.cpp
+++ bitcoin-knots-28.1.knots20250305/src/clientversion.cpp
@@ -32,8 +32,7 @@ const std::string CLIENT_NAME("Satoshi")
 //   - "// No build information available", if proper git information is not available
 #endif
 
-//! git will put "#define GIT_COMMIT_ID ..." on the next line inside archives. 
-#define GIT_COMMIT_ID "5f8256608fc41c7b72a01a54e9a06ba509a61c8f"
+//! git will put "#define GIT_COMMIT_ID ..." on the next line inside archives. $Format:%n#define GIT_COMMIT_ID "%H"$
 
 #ifdef BUILD_GIT_TAG
     #define BUILD_DESC BUILD_GIT_TAG
